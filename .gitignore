# Internal planning docs (specs/plans) -- never commit to a public repo
docs/superpowers/
# Internal research references (public facts, but kept local until an OSS release pass)
docs/research/
PRIOR-ART.md

# Synthetic corpora + trained models (large, regenerable; never ship data)
datasets/
models/
*.jsonl

# Media / screenshots (promo + landing assets, not model/code IP)
*.mp4
*.png

# Python / venv / node
__pycache__/
.venv*/
.pytest_cache/
node_modules/

# Backups + scratch
*.bak*
*.ok-*
*.tmp
/tmp/

# Real-document (PII) validation work area -- raw text + per-doc offsets stay LOCAL only.
# The committed artifact is validation/RESULT-realworld-*.md (aggregate, PII-free).
validation/realworld/
expenses-eval/
*.pii.json
*.pii.txt

# Local agent + tooling scratch (worktrees, MCP browser snapshots) -- never commit
.claude/
.playwright-mcp/

# Appliance runtime data + artifacts (Beelink egress proxy -- never commit)
# SENSITIVE: encrypted session entity maps + .mapkey AES key
maps/
# model weights (singular dir on the host)
model/
# int8 model weights
model-v11r5-base-int8/
# same-disk model backups
model.bak-*/
# OpenVINO compile cache
.ovcache/
# runtime/private dir
private/
# live host-specific config (commit gateway-config.example.yaml instead)
gateway-config.yaml
