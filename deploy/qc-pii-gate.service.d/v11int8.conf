# Drop-in: repoint qc-pii-gate to the v11r5-base ONNX-INT8 CPU tier (gate_service_cpu.py).
# Replaces the v7 OpenVINO/NPU OVTier. Revert = remove this file + daemon-reload + restart
# (the base unit's original ExecStart -> gate_service.py / OVTier is preserved).
[Service]
ExecStart=
ExecStart=/home/steven/sparx-npu/.venv/bin/python /home/steven/sparx-npu/gate_service_cpu.py
Environment=CPU_GATE_PORT=8001
Environment=CPU_GATE_MODEL=/home/steven/sparx-npu/model-v11r5-base-int8
Environment=CUDA_VISIBLE_DEVICES=
