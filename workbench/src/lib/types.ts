// Re-export from the canonical single source: @ossredact/core
// The source of truth for these types lives in packages/redaction-core/src/types.ts
export type { RawSpan, Span, EntityMap, RegionBox } from '@ossredact/core'
